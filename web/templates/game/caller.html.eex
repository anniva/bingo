<h2>Caller for <%= @game.name %></h2>

<ul>

  <li>
    <strong>Name:</strong>
    <%= inspect @game %>
  </li>

  <button id='new-number'>
    CALL NUMBER
  </button>

</ul>

<%= link "Edit", to: game_path(@conn, :edit, @game) %>
<%= link "Back", to: game_path(@conn, :index) %>
